(define (last-pair x)
  (if (null? (cdr x)) x (last-pair (cdr x))))

(define (make-cycle x)
  (set-cdr! (last-pair x) x)
  x)
(define z (make-cycle '(a b c)))
;#0=(a b c . #0#)
z

so z is a cycle '(a b c a b c a b c ....)
(last-pair z) is running forever.
